import { FiltroTurnosAceptadosPipe } from './filtro-turnos-aceptados.pipe';

describe('FiltroTurnosAceptadosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosAceptadosPipe();
    expect(pipe).toBeTruthy();
  });
});
